# Core dependencies (always needed)
Pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.9.0.80

# Optional: for higher accuracy with dlib backend
# face-recognition==1.3.0  # Requires dlib (C++ build tools)
# dlib==19.24.2            # Requires Visual Studio C++ on Windows
